<form method="post" action="{{ path('hwack_delete', {'id': hwack.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hwack.id) }}">
    <button class="btn">Delete</button>
</form>
