{% extends 'base.html.twig' %}

{% block title %}Hwack{% endblock %}

{% block body %}
    <h1>Hwack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hwack.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ hwack.content }}</td>
            </tr>
            <tr>
                <th>IsPrivate</th>
                <td>{{ hwack.isPrivate ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ hwack.createdAt ? hwack.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hwack_index') }}">back to list</a>

    <a href="{{ path('hwack_edit', {'id': hwack.id}) }}">edit</a>

    {{ include('hwack/_delete_form.html.twig') }}
{% endblock %}
